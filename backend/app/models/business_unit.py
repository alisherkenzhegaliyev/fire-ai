# SQLAlchemy ORM model: business_units table (offices)
# Columns (from CSV):
#   id (UUID PK), session_id (str, indexed), name (str), address (str),
#   latitude (Float nullable), longitude (Float nullable)
#   (lat/lon geocoded from address for distance calculations)
