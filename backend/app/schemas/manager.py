# Pydantic schemas for manager API
# Classes:
#   ManagerCreate: fields from CSV row (used internally by csv_parser)
#   ManagerResponse: full response schema (all manager fields + workload count for session)
